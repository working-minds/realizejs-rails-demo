<%=
  render 'form', locals: {
    title: 'Editing Project',
    httpMethod: 'PUT',
    action: project_path(@project.id),
    otherButtons: [
      {
        name: 'Back',
        style: :cancel,
        element: :a,
        href: projects_path
      },
      {
        name: 'Issues',
        style: :cancel,
        element: :a,
        icon: :assignment,
        href: project_issues_path(@project.id)
      }
    ]
  }
%>