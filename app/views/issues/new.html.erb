<%=
  render 'form', locals: {
    title: "New #{@project.name} Issue",
    httpMethod: 'POST',
    action: project_issues_path(@project.id),
    otherButtons: [
      {
        name: 'Back',
        style: :cancel,
        element: :a,
        href: project_issues_path(@project.id)
      }
    ]
  }
%>