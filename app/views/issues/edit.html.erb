<%=
  render 'form', locals: {
    title: "Editing #{@project.name} Issue",
    httpMethod: 'PUT',
    action: project_issue_path(@project.id, @issue.id),
    otherButtons: [
      {
        name: 'Back',
        style: :cancel,
        element: :a,
        href: project_issues_path(@project.id)
      }
    ]
  }
%>
